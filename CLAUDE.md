# Claude Code notes

Follow `AGENTS.md` (conventions, commands, layout). Before finishing a change run `npm run verify`; for anything touching gestures or layout also run `npm run test:e2e` and inspect a phone-sized screenshot.

Docs live in `docs/` (bugs, architecture, roadmap, per-feature plans). Feature plans list open questions — ask the maintainer instead of assuming.
