# Plan: internationalisation

Status: **shipped** for `en` and `pt-BR` (UI strings, spread names/positions, card names/keywords/meanings, auto-detected from `navigator.language`, switchable in the menu).

## Next
- More locales (es, fr, it, de): add a column to `messages` and to the card table; the unit test enforces key parity.
- Number/date formatting via `Intl` in the journal.
- `<html lang>` updated at runtime; document title localized.
- Extract to JSON per locale if the tables grow, with a build-time check.
